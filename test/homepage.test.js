describe("Checking if website launches as expected", () => {
  test("Sanity check, should always pass", () => {
    expect(2 + 2 == 4);
  });

  test("website has navigation", () => {
    expect(true); //TODO: Check if nav element exists
  });

  test("Check hero element", () => {
    expect(true); //TODO: Check if H1 exists
  });

  test("website has footer", () => {
    expect(true); //TODO: Check if footer element exists
  });
});

describe("Send example mail", () => {
  test("Send email", () => {
    expect(true); // Send mail by emulating a user
  });
});